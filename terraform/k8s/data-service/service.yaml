apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: zenalpha
  labels:
    app: data-service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
  selector:
    app: data-service
