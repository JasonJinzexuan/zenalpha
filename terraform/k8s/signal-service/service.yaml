apiVersion: v1
kind: Service
metadata:
  name: signal-service
  namespace: zenalpha
  labels:
    app: signal-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: signal-service
