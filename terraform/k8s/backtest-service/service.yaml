apiVersion: v1
kind: Service
metadata:
  name: backtest-service
  namespace: zenalpha
  labels:
    app: backtest-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: backtest-service
