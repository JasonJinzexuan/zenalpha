apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: zenalpha
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
      name: http
  selector:
    app: user-service
